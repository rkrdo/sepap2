<!DOCTYPE html>
<!-- paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither/ -->
<!--[if lt IE 7 ]> <html class="ie6" lang="en"> <![endif]-->
<!--[if IE 7 ]>    <html class="ie7" lang="en"> <![endif]-->
<!--[if IE 8 ]>    <html class="ie8" lang="en"> <![endif]-->
<!--[if (gte IE 9)|!(IE)]><!--> <html lang="en"> <!--<![endif]-->
<head>
	<meta charset="utf-8" />

	<!-- Uncomment to make IE8 render like IE7 -->
	<!-- <meta http-equiv="X-UA-Compatible" content="IE=7" /> -->

	<!-- Set the viewport width to device width for mobile -->
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />

	<title><%= content_for?(:title) ? "Tutoring 24/7 | #{yield(:title)}" : "Tutoring 24/7" %></title>

	<!-- Included CSS Files -->
	<%= stylesheet_link_tag "application" %>
  <%= admin_stylesheets %>
	<!-- Included JS Files  -->
	<%= javascript_include_tag "application" %>

  <%= csrf_meta_tags %>
</head>
<body>

  <%= hidden_field_tag "current_locale", params[:locale] %>
  <%#header%>
  <div class="row hide-for-small" id="barra-arriba" >
    <br />
    <%= link_to image_tag("logo.png"), root_path(:locale => params[:locale]) %>
  </div>
  <nav class="top-bar">
      <ul>
        <li class="name show-for-small"><h1><%= link_to 'Tutoring 24/7', root_path, :style => "font-size:17px;" %></h1></li>
        <li class="toggle-topbar"><a href="#"></a></li>
      </ul>
      <section>
        <%= render @menu %>
      </section>
  </nav>
  <%#flash notices%>
  <div class="row">
    <div class="twelve columns">
    <% flash.each do |key, value| %>
      <%= content_tag(:div, class: "alert-box #{key}") do %>
        <%= value %>
        <%= content_tag(:a,"x", class:"close") %>
      <% end %>
    <% end %>
    </div>
  </div>

  <%#main content%>
  <div class="row">
    <div class="twelve columns">
      <%= yield %>
    </div>
  </div>
  <div class="reveal-modal">
    <div class="modal-content">
    </div>
    <div class="row" id="reveal-loading">
      <div class="one columns centered end">
        <i class="icon-spinner icon-spin icon-3x"></i>
      </div>
    </div>
    <a class="close-reveal-modal">&#215;</a>
  </div>
  <%#footer%>
  <footer>
    <div class="row">
      <div class="twelve columns">
        <hr>
        <div class="row">
          <div class="six columns">
            <p>Tutoring 24/7</p>
          </div>

          <div class="six columns">
            <ul class="link-list right">
              <li><%= image_tag 'tec_logo_small.png' %></li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </footer>
</body>
</html>
