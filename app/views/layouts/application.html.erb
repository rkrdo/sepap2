<!DOCTYPE html>
<!-- paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither/ -->
<!--[if lt IE 7 ]> <html class="ie6" lang="en"> <![endif]-->
<!--[if IE 7 ]>    <html class="ie7" lang="en"> <![endif]-->
<!--[if IE 8 ]>    <html class="ie8" lang="en"> <![endif]-->
<!--[if (gte IE 9)|!(IE)]><!--> <html lang="en"> <!--<![endif]-->
<head>
	<meta charset="utf-8" />

	<!-- Uncomment to make IE8 render like IE7 -->
	<!-- <meta http-equiv="X-UA-Compatible" content="IE=7" /> -->

	<!-- Set the viewport width to device width for mobile -->
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />

	<title><%= content_for?(:title) ? yield(:title) : "SEPAP" %></title>

	<!-- Included CSS Files -->
	<%= stylesheet_link_tag "application" %>
  <%= admin_stylesheets %>
	<!-- Included JS Files  -->
	<%= javascript_include_tag "application" %>

  <%= csrf_meta_tags %>
</head>
<body>

  <%= hidden_field_tag "current_locale", params[:locale] %>
  <%#header%>
  <div class="row hide-for-small" id="barra-arriba" >
    <%= link_to image_tag("logo.png"), root_path(:locale => params[:locale]) %>
  </div>
  <nav class="top-bar">
      <ul>
        <li class="name show-for-small"><h1><%= link_to 'SEPAP', root_path, :style => "font-size:17px;" %></h1></li>
        <li class="toggle-topbar"><a href="#"></a></li>
      </ul>
      <section>
        <%= render @menu %>
      </section>
  </nav>
  <%#flash notices%>
  <div class="row">
    <% flash.each do |key, value| %>
      <%= content_tag(:div, class: "alert-box #{key}") do %>
        <%= value %>
        <%= content_tag(:a,"x", class:"close") %>
      <% end %>
    <% end %>
  </div>

  <%#main content%>
  <div class="row">
    <%= yield %>
  </div>
  <div class="reveal-modal">
    <div class="modal-content">
    </div>
    <a class="close-reveal-modal">&#215;</a>
  </div>
  <%#footer%>
  <footer>
    <hr />
    <div>
      <div class="six columns">
        <p>SEPAP.</p>
      </div>

      <div class="six columns">
        <ul class="link-list right">
        </ul>
      </div>
    </div>
  </footer>
</body>
</html>
