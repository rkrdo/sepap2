<h1>Listing Assignments</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @assignments.each do |assignment| %>
  <tr>
    <td><%= link_to 'Show', [:teacher, assignment] %></td>
    <td><%= link_to 'Edit', edit_teacher_assignment_path(assignment) %></td>
    <td><%= link_to 'Destroy', [:teacher, assignment], confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Assignment', new_teacher_assignment_path %>
