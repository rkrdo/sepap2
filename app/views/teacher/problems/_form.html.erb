<%= form_for([:teacher, @teacher_problem]) do |f| %>
  <% if @teacher_problem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@teacher_problem.errors.count, "error") %> prohibited this teacher_problem from being saved:</h2>

      <ul>
      <% @teacher_problem.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
